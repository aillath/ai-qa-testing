
    [
      {
        "id": "${{ github.event.client_payload.itemId }}",
        "name": "${{ github.event.client_payload.pageName }}",
        "url": "${{ github.event.client_payload.url }}",
        "type": "${{ github.event.client_payload.type }}"
      }
    ]